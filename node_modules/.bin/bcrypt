#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_bcryptjs@2.4.4@bcryptjs/bin/bcrypt" "$@"
  ret=$?
else 
  node  "$basedir/../_bcryptjs@2.4.4@bcryptjs/bin/bcrypt" "$@"
  ret=$?
fi
exit $ret
